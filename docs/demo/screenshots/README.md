# FinRoot UI Screenshots — Demo Assets

> Generated by `scripts/capture_screenshots.py`. Each screenshot demonstrates a
> specific judging axis for the SCALE ML Club PS-1 submission.

| Screenshot | What it shows | Judging axis |
|---|---|---|
| `01_chat_portfolio.png` | **Chat tab** with a real portfolio-risk query answered. Shows the structured finance card: summary, confidence badge, risk profile, analysis, actions, risks, alternatives, and citations. | Reasoning Quality (35%), Agent Architecture (30%) |
| `02_reasoning_trace.png` | **Reasoning Trace tab** for the same answer. Shows the step-by-step plan, tool calls, 5-axis self-critic scores (with pass/fail + must-fix items), prudential principles verifier verdict, and a citations table. This is the "show your work" transparency feature. | Reasoning Quality (35%), Agent Architecture (30%) |
| `03_trap_refusal.png` | **Chat tab** responding to a trap question ("put emergency fund into hot small-cap stock"). Demonstrates the agent's prudence — it refuses unsafe advice, flags risk, and explains why, rather than blindly complying. | Reasoning Quality (35%), Solution Idea (15%) |
| `04_digital_twin.png` | **Digital Twin tab** displaying the investor profile: age, risk tolerance, investment horizon, tax bracket, monthly finances, goals, constraints, portfolio holdings table, allocation bar chart, and total portfolio value. Shows personalization. | Solution Idea (15%), Code Implementation (20%) |
| `05_harness.png` | **Harness tab** showing the FRB benchmark runner: composite lift vs RAG metric, system comparison table (finroot / RAG / single-agent across Pass@1, Pass@k, Pass^k, Mean Score), and per-domain mean-score bar chart. Numbers from `results/metrics.json`. | Agent Architecture (30%), Code Implementation (20%) |

## How to regenerate

```bash
pip install playwright && playwright install chromium
PYTHONPATH=src python3 scripts/capture_screenshots.py
```

The script requires Chromium (via Playwright). All screenshots are rendered in
Mock mode (`FINROOT_LLM_PROVIDER=mock`) — no API keys needed.
